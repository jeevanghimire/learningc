#include <stdio.h>
int bitwise(int n)
{
    int i;
    for (i =n ; i < 0; i++)
    {
       
        printf("%d", n);
    }
}
int main()
{
    int n = 13;
    printf("%d", bitwise(n));
    return 0;
}