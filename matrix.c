#include <stdio.h>

int main()
{
    int matrixA[10], [10], m1, m2;
    int matrixB[10], [10], b1, b2;
    int i, j;
    printf("Enter the number for martixA;");
    scanf("%d%d", &m1, &m2);
    printf("Enter the dimension for matrixB;");
    scanf("%d%d", &b1, &b2);
    for (i = 0; i < ; i++)
    {
        
    }

    return 0;
}